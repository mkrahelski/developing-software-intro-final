//vertical studs tally.


function studsCalc(length:number, width:number)
    
    {
        const effectiveL = length - (((Math.ceil(length/8)))*3.5)
        const effectiveW = width - (((Math.ceil(width/8)))*3.5)

        effectiveL 

        effectiveW


    }